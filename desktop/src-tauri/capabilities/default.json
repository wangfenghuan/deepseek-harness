{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the launcher windows: the splash and settings pages use invoke + events (core window management and event listening); the remote dsh web page loaded into the main window uses no Tauri IPC beyond the same small custom command set.",
  "windows": ["main", "settings"],
  "permissions": ["core:default"]
}
