{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the main window: the remote dsh web page uses no Tauri IPC, so only core window management is granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
